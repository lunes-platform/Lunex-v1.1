---
phase: quick-260616-p93
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Lunex/contracts/listing_manager/src/lib.rs
  - Lunex/contracts/listing_manager/Cargo.toml
  - Lunex/contracts/listing_manager/README.md
autonomous: true
requirements: [B1]

must_haves:
  truths:
    - "list_token performs a real cross-contract create_lock call to self.liquidity_lock (no more lock_id = listing_id stub)"
    - "On lock-creation failure, list_token returns Err(LockCreationFailed) and stores NO ListingRecord (atomicity: no orphan Active listing)"
    - "The lock_id saved in ListingRecord and emitted in TokenListed is the value returned by create_lock, not listing_id"
    - "listing_manager unit tests pass under rustup 1.85.0 cargo test"
    - "Deployment order (liquidity_lock -> set_manager(listing_manager) -> listing_manager::new) is documented"
  artifacts:
    - path: "Lunex/contracts/listing_manager/src/lib.rs"
      provides: "Step 4 build_call to create_lock; lock_id from return value"
      contains: "selector_bytes!(\"create_lock\")"
    - path: "Lunex/contracts/listing_manager/Cargo.toml"
      provides: "e2e-tests feature + ink_e2e dev-dependency (Part B; may be deferred)"
    - path: "Lunex/contracts/listing_manager/README.md"
      provides: "deployment order documentation"
  key_links:
    - from: "listing_manager::list_token (Step 4)"
      to: "liquidity_lock::create_lock"
      via: "build_call exec_input + Selector(create_lock)"
      pattern: "selector_bytes!\\(\"create_lock\"\\)"
    - from: "Step 5 ListingRecord.lock_id / TokenListed.lock_id"
      to: "create_lock return value"
      via: "let lock_id = <call>.map_err..?.map_err..?"
      pattern: "lock_id"
---

<objective>
Enforce the LP lock on-chain. `listing_manager` Step 4 currently fakes the lock with
`let lock_id: u64 = listing_id;` and never calls `liquidity_lock::create_lock` — a direct
rug-pull vector (B1, P0). Replace Step 4 with a real cross-contract `build_call` to
`create_lock`, use the returned `lock_id` in Step 5, and make the call atomic (no orphan
Active listing on lock failure). Then stand up an ink-e2e harness to prove the happy path.

Purpose: Close the custody gap so a listing cannot be marked Active without the LP actually
being locked in `liquidity_lock` storage.

Output:
- Part A (MUST land, commit independently): contract fix + honest unit coverage.
- Part B (TIMEBOX, fallback allowed): ink-e2e harness + happy-path test, or documented-pending.
- Deployment-order documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/blockers-roadmap-2026-06-16/01-lp-lock-not-enforced.md

# THE FIX SITE (read before editing)
@Lunex/contracts/listing_manager/src/lib.rs
@Lunex/contracts/liquidity_lock/src/lib.rs

<interfaces>
<!-- Extracted from the codebase. Use these directly; no exploration needed. -->

listing_manager/src/lib.rs — list_token signature (line 330):
```
pub fn list_token(
    &mut self,
    tier:            u8,
    token_address:   AccountId,
    pair_address:    AccountId,
    lp_token:        AccountId,
    lp_amount:       Balance,
    lunes_liquidity: Balance,
    token_liquidity: Balance,
) -> Result<ListingId>
```
Locals available at Step 4: `caller` (= self.env().caller(), line 343), `cfg` (TierConfig,
line 342 — `cfg.lock_duration_ms` is the lock duration), `listing_id` (line 359), plus all
the params above. Target address: `self.liquidity_lock` (storage, line 187).

listing_manager Error enum (line 265): `Error::LockCreationFailed` ALREADY EXISTS (line 274).
Do NOT re-add it. `pub type Result<T> = core::result::Result<T, Error>;` (line 283).

Existing analog — PSP22Ref::transfer (lines 87-108). It decodes the foreign contract's inner
error into a LOCALLY-declared mirror enum `PSP22Error` (lines 67-76) — NOT by importing the
token crate. It uses `.returns::<core::result::Result<(), PSP22Error>>().try_invoke()` then
double-`map_err(|_| Error::TransferFailed)`. This local-mirror pattern is exactly what Step 4
must reuse. build_call, ExecutionInput, Selector, DefaultEnvironment already imported (lines 35-36).

liquidity_lock/src/lib.rs — create_lock signature (line 136):
```
pub fn create_lock(
    &mut self,
    owner:            AccountId,
    pair_address:     AccountId,
    lp_token:         AccountId,
    lp_amount:        Balance,
    lunes_amount:     Balance,
    token_amount:     Balance,
    lock_duration_ms: u64,
    tier:             u8,
) -> Result<LockId>          // LockId = u64 (line 22); Result = core::result::Result<T, Error> (line 99)
```
liquidity_lock's Error enum (lines 88-97) variants IN ORDER:
`Unauthorized, LockNotFound, LockNotExpired, AlreadyWithdrawn, ZeroAmount, InvalidTier, TransferFailed`.
Guarded by `ensure_manager()` (line 147): the listing_manager contract address MUST be
registered via `set_manager` (line 119) before this call succeeds.
Getter: `get_lock(&self, lock_id: LockId) -> Option<LockRecord>` (line 262).

Step 4 to REPLACE (lines 415-420):
```
// ── Step 4: register lock ──
// ...off-chain relayer comment...
let lock_id: u64 = listing_id;
```
Step 5 (lines 422-450) reads `lock_id` into ListingRecord.lock_id and TokenListed.lock_id —
leave Step 5 structure intact, it already uses the `lock_id` variable.

CRITICAL ORDERING CONSTRAINT (verified against existing tests): in ink! off-chain unit tests,
list_token reaches Step 1 (PSP22Ref::transfer_from, line 364) BEFORE Step 4. Cross-contract
calls to non-deployed addresses fail/panic off-chain. The existing unit tests
(`cannot_list_same_token_twice_via_mapping`, `insufficient_liquidity_rejected`,
`zero_amounts_rejected`, `paused_contract_rejects_listing` — lines 662-750) ONLY exercise
pre-condition logic that runs BEFORE Step 1. Therefore neither the create_lock success path
NOR the create_lock failure path is reachable in a unit test — both require both contracts
deployed (Part B / ink-e2e). The spec confirms this (TDD note, lines 199-205).
</interfaces>

# Toolchain (from CLAUDE.md / STATE.md)
- Use `rustup run 1.85.0 cargo ...`. Homebrew cargo 1.94 is INCOMPATIBLE — do not use plain `cargo`.
- ink! 4.2.1 for BOTH contracts. rustfmt: `.rustfmt.toml` (max_width 100, vertical imports, trailing_semicolon=false).
- substrate-contracts-node is installed at ~/.local/bin (for Part B).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Part A): Replace Step 4 stub with real create_lock cross-contract call, atomic on failure</name>
  <files>Lunex/contracts/listing_manager/src/lib.rs</files>
  <behavior>
    - lock_id is no longer assigned `= listing_id`; it is the value returned by create_lock.
    - On create_lock failure, list_token returns Err(LockCreationFailed) and inserts nothing
      into self.listings / self.token_listing and emits no TokenListed (atomicity: the build_call
      result is mapped before Step 5, so the `?` short-circuits before any insert/emit).
    - Steps 1-3 (fee collection + split), fee BPS constants, and ListingRecord shape are unchanged.
    - lock_id stays u64; do NOT assert lock_id == listing_id anywhere.
  </behavior>
  <action>
    Per blocker spec B1-T1/B1-T3 (.planning/blockers-roadmap-2026-06-16/01-lp-lock-not-enforced.md).

    Error variant: `Error::LockCreationFailed` ALREADY EXISTS at line 274 — do NOT re-add it.

    Decode strategy (reuse the file's own pattern, do NOT import liquidity_lock): the PSP22Ref
    analog already proves the correct technique — it decodes the foreign contract's inner error
    into a LOCAL mirror enum (`PSP22Error`, lines 67-76) and double-map_errs. Do the same here:
    1. Add a local mirror enum next to PSP22Error (e.g. `LiquidityLockError`) deriving
       `(Debug, PartialEq, Eq, Encode, Decode)` + the std `scale_info::TypeInfo` cfg_attr, with the
       SAME variants in the SAME ORDER as liquidity_lock::Error (see &lt;interfaces&gt;:
       Unauthorized, LockNotFound, LockNotExpired, AlreadyWithdrawn, ZeroAmount, InvalidTier,
       TransferFailed). Order matters: SCALE decodes enums by index. The error value is discarded
       (map_err to LockCreationFailed), so the mirror exists only to make the inner Result decodable.
    2. Replace Step 4 (lines 415-420) with:
       `build_call::<DefaultEnvironment>().call(self.liquidity_lock).gas_limit(0).transferred_value(0)`
       `.exec_input(ExecutionInput::new(Selector::new(ink::selector_bytes!("create_lock")))` then
       push the 8 positional args IN ORDER matching create_lock: `caller` (owner), `pair_address`,
       `lp_token`, `lp_amount`, `lunes_liquidity` (lunes_amount), `token_liquidity` (token_amount),
       `cfg.lock_duration_ms`, `tier`. Match PSP22Ref's push_arg style.
       `.returns::<core::result::Result<u64, LiquidityLockError>>().try_invoke()`
       then `.map_err(|_| Error::LockCreationFailed)?` (outer LangError)
       then `.map_err(|_| Error::LockCreationFailed)?` (inner contract Result) → binds `lock_id: u64`.
    3. Bind `lock_id` BEFORE Step 5 so the `?` returns early on failure (no insert / no emit).

    Run `rustup run 1.85.0 cargo check -p listing_manager` to confirm the decode type resolves; iterate
    only on the return-type detail if needed (keep the local-mirror approach; do NOT add a contract
    dependency). Do NOT touch Steps 1-3, fee BPS, ListingRecord shape, or Step 5's use of `lock_id`.
    Then `rustup run 1.85.0 cargo fmt -p listing_manager` (rustfmt unstable per .rustfmt.toml).
  </action>
  <verify>
    <automated>cd /Users/lucas/Documents/Projetos_DEV/Lunex/Lunex/contracts/listing_manager && grep -q 'selector_bytes!("create_lock")' src/lib.rs && ! grep -q 'let lock_id: u64 = listing_id' src/lib.rs && rustup run 1.85.0 cargo test 2>&1 | grep -E 'test result: ok'</automated>
  </verify>
  <done>`let lock_id: u64 = listing_id;` is gone; a build_call to create_lock with the 8 correct args (decoded via a local mirror error enum, no contract dependency) is in Step 4; lock_id comes from the call return; failure maps to Err(LockCreationFailed) before any insert/emit; `rustup run 1.85.0 cargo test -p listing_manager` is green (existing pre-condition tests still pass).</done>
</task>

<task type="auto">
  <name>Task 2 (Part A): Honest unit coverage + document why success/failure paths are e2e-only</name>
  <files>Lunex/contracts/listing_manager/src/lib.rs</files>
  <action>
    Per spec TDD note (lines 199-205) and the verified ordering constraint in &lt;interfaces&gt;:
    in off-chain unit tests, list_token fails at Step 1 (PSP22Ref::transfer_from to a non-deployed
    address) BEFORE ever reaching Step 4, so the create_lock success AND failure paths are NOT
    unit-testable. Do NOT write a unit test that claims to assert Err(LockCreationFailed) by calling
    list_token end-to-end — it would actually trip on TransferFailed (or panic) at Step 1 and give a
    false signal.

    Deliverable: add a `//` comment block in the `#[cfg(test)]` section stating that list_token's
    Step 4 lock-creation path requires both contracts deployed and is proven in the ink-e2e harness
    (Task 3); off-chain unit tests can only cover pre-Step-1 pre-conditions. Reference the spec lines.
    Keep all existing unit tests (lines 662-750) passing. Add a new unit test ONLY if it asserts
    something genuinely reachable and new (discipline: no test that passes for the wrong reason). If
    there is no real new assertion, the comment block is the deliverable and Task 3 carries the
    happy-path proof. Run `rustup run 1.85.0 cargo fmt -p listing_manager`.
  </action>
  <verify>
    <automated>cd /Users/lucas/Documents/Projetos_DEV/Lunex/Lunex/contracts/listing_manager && rustup run 1.85.0 cargo test 2>&1 | grep -E 'test result: ok'</automated>
  </verify>
  <done>A comment in the test module documents that Step 4 lock-creation is e2e-only and why (Step 1 precedes Step 4 off-chain); all existing unit tests still pass; no false-green LockCreationFailed test. COMMIT Part A here (Tasks 1+2) independently — message: `fix(listing_manager): enforce LP lock on-chain via create_lock cross-contract call (B1)`.</done>
</task>

<task type="auto">
  <name>Task 3 (Part B, TIMEBOX): ink-e2e harness + happy-path test, or documented-pending fallback</name>
  <files>Lunex/contracts/listing_manager/Cargo.toml, Lunex/contracts/listing_manager/src/lib.rs, Lunex/contracts/listing_manager/README.md</files>
  <action>
    Per spec B1-T4/B1-T5. Part A is ALREADY COMMITTED — Part B must never block or revert it.

    TIMEBOX this task. If ink-e2e setup fights the environment (ink_e2e version/compat with 4.2.1,
    substrate-contracts-node wiring, build issues), STOP rat-holing. Do the FALLBACK (below) and end
    the task. Do not modify Part A code to make e2e pass.

    Attempt:
    1. Cargo.toml: add `e2e-tests = []` under `[features]` and `[dev-dependencies] ink_e2e = "4.2.1"`
       (match ink 4.2.1). The harness deploys both contracts' artifacts.
    2. Write an ink-e2e test (gated `#[cfg(all(test, feature = "e2e-tests"))]`, `#[ink_e2e::test]`)
       that: instantiates liquidity_lock, instantiates listing_manager with that liquidity_lock
       address (+ the other 4 addresses), calls `liquidity_lock.set_manager(listing_manager_addr)`,
       then drives list_token with lp_amount > 0 (satisfying the Steps 1-3 PSP22 fee prerequisites in
       the e2e env — deploy/approve a PSP22 LUNES token; if the full fee path is too heavy for the
       timebox, narrow the assertion to lock creation), then asserts
       `liquidity_lock.get_lock(returned_lock_id).is_some()`. Do NOT assert lock_id == listing_id.
    3. Run: `rustup run 1.85.0 cargo test --features e2e-tests` against the local
       substrate-contracts-node (~/.local/bin). Start the node if needed.

    FALLBACK (explicit, allowed): if any step fails after a focused attempt, REVERT/guard the Part B
    changes so the default `rustup run 1.85.0 cargo test` stays green, and write a `## E2E: PENDING`
    section in README.md documenting: (a) the exact blocker (version/compat/node error text),
    (b) the intended test shape from step 2, (c) that Part A is committed and proven by `cargo test`
    + code review. Happy path remains proven-by-construction (mirrors the working PSP22Ref analog).

    ALWAYS (pass or fallback): add a deployment-order section to README.md per B1-T5:
    deploy `liquidity_lock` -> `liquidity_lock.set_manager(listing_manager_addr)` ->
    `listing_manager::new(lunes_token, liquidity_lock, treasury, rewards_pool, staking_pool)`.
    Note the pre-condition that the lister must have approved the listing fee.
  </action>
  <verify>
    <automated>cd /Users/lucas/Documents/Projetos_DEV/Lunex/Lunex/contracts/listing_manager && grep -qiE 'deploy|set_manager' README.md && rustup run 1.85.0 cargo test 2>&1 | grep -E 'test result: ok'</automated>
  </verify>
  <done>EITHER: ink-e2e happy-path passes via `rustup run 1.85.0 cargo test --features e2e-tests` (get_lock(lock_id).is_some()); OR: e2e documented as PENDING in README.md with the exact blocker AND the default `cargo test` is still green (no broken e2e in the default build). In BOTH cases README.md documents deployment order. COMMIT Part B separately — `test(listing_manager): ink-e2e LP-lock happy path + deploy-order docs (B1)` (or `docs(listing_manager): document LP-lock deploy order; e2e pending (B1)` on fallback).</done>
</task>

</tasks>

<verification>
- `grep -q 'selector_bytes!("create_lock")' Lunex/contracts/listing_manager/src/lib.rs` (real call wired)
- `! grep -q 'let lock_id: u64 = listing_id' Lunex/contracts/listing_manager/src/lib.rs` (stub gone)
- `cd Lunex/contracts/listing_manager && rustup run 1.85.0 cargo test` => `test result: ok` (Part A green)
- Part A committed independently of Part B.
- README.md documents deployment order.
- No test passes for the wrong reason (no off-chain LockCreationFailed assertion that actually trips at Step 1).
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| token lister -> listing_manager.list_token | untrusted caller initiates a fund-moving listing |
| listing_manager -> liquidity_lock.create_lock | privileged cross-contract call gated by ensure_manager |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-b1-01 | Tampering | Step 4 fake lock (lock_id = listing_id) | mitigate | Replace with real create_lock build_call; lock_id from return value (Task 1) |
| T-b1-02 | Repudiation/Integrity | Active listing with no actual lock | mitigate | Atomic failure: map_err before Step 5 so no ListingRecord/TokenListed on lock failure (Task 1) |
| T-b1-03 | Elevation of Privilege | create_lock callable by non-manager | accept (out of scope) | liquidity_lock.ensure_manager already gates; deploy-order doc ensures correct manager (Task 3 README) |
| T-b1-04 | Information Disclosure | SubQuery shows lock_id as "locked" when not | mitigate | Fixed transitively: lock_id now reflects a real lock (Task 1) |
| T-b1-SC | Tampering | ink_e2e dev-dependency install (crates.io) | mitigate | ink_e2e is part of the official ink! 4.2.1 toolchain (paritytech), pinned to match ink 4.2.1; no new untrusted package |
</threat_model>

<success_criteria>
- list_token calls liquidity_lock::create_lock on-chain; the stub `lock_id = listing_id` is removed.
- On lock failure, no orphan Active listing is created (atomic).
- ListingRecord.lock_id / TokenListed.lock_id come from the create_lock return value.
- `rustup run 1.85.0 cargo test -p listing_manager` is green; Part A committed independently.
- Happy path proven by ink-e2e (get_lock(lock_id).is_some()) OR documented as PENDING with the exact blocker, without breaking the default test build.
- Deployment order documented in README.md.
- BOUNDARY respected: no changes to Steps 1-3, fee BPS, ListingRecord shape (except lock_id source), no LiquidityLockRef contract dependency, no frontend/SDK changes, no lock_id == listing_id assertions.
</success_criteria>

<output>
Create `.planning/quick/260616-p93-b1-lp-lock-enforce-create-lock-on-chain-/260616-p93-SUMMARY.md` when done.
</output>
