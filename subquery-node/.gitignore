# Build artifacts (generated by `subql build`)
dist/

# Auto-generated by `subql codegen` — derived from schema.graphql.
src/types/

# Dependencies
node_modules/

# Local DB / cache
.subql/
.cache/

# Renderable project.yaml is regenerated at runtime by entrypoint.sh —
# we still commit it so local IDE tooling (subql lsp, codegen) has a parseable
# file, just with dev defaults that production overrides.
