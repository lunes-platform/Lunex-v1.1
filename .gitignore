# ═══════════════════════════════════════════════════════════════
# Lunex DEX — .gitignore
# ═══════════════════════════════════════════════════════════════

# ── Dependencies ──
node_modules/
package-lock.json
yarn.lock

# ── Build artifacts ──
**/target/
/artifacts/
dist/
build/
.next/
/types/
*.rs.bk

# ── Environment & Secrets ──
.env
.env.local
.env.development
.env.production
.env.staging
.env.test
.env.docker
**/.env
!**/.env.example
!**/.env.*.example

# ── IDE & Editors ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── AI Agent / IDE Config (DO NOT COMMIT) ──
.agent/
.gemini/
.cursor/
.copilot/
.aider*
.continue/
GEMINI.md

# ── OS generated ──
.DS_Store
Thumbs.db
*.log

# ── Docker data volumes ──
docker/data/
docker/certbot/
docker/ssl/

# ── Test / Coverage ──
coverage/
.nyc_output/
*.lcov

# ── Miscellaneous ──
*.tgz
*.tar.gz
tmp/

# ── Subprojects (separate repos) ──
lunex-admin/
subquery-node/
